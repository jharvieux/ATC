[
  {
    "_schema": "Each entry: { route, method, reason } — routes here are exempt from the cross-tenant probe.",
    "_example": {
      "route": "/api/admin/tenants",
      "method": "GET",
      "reason": "Platform-admin route; intentionally cross-tenant. Requires platform-admin role, not tenant user."
    }
  },
  {
    "route": "/api/health",
    "method": "GET",
    "reason": "Public health sentinel; validated separately for a 200 response and a concrete hosted commit before protected routes are probed."
  },
  {
    "route": "/api/inngest",
    "method": "PUT",
    "reason": "Inngest control-plane registration endpoint; probing it performs an out-of-band registration rather than a tenant-resource read."
  }
]
