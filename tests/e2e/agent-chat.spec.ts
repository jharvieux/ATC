import { test } from "@playwright/test";

// Persona chat on the public landing page (§7.2).
// TODO(#459): write E2E coverage; API exists, browser test needs live endpoint wiring.

test.fixme("chat with Marcus Cole returns a response", async () => {});
test.fixme("chat with Maya Patel returns a response", async () => {});
test.fixme("chat handles empty input gracefully", async () => {});
