import { test } from "@playwright/test";

// Customer self-service portal (§7.2) — not yet implemented.
// TODO(#459): write E2E coverage once the customer-portal routes ship.

test.fixme("customer can sign in and view their trips", async () => {});
test.fixme("customer profile page loads and is editable", async () => {});
