import { test } from "@playwright/test";

// Self-service help module — search, article view, bug-report flow (§7.2).
// TODO(#459): write E2E coverage once help-module UI is wired.

test.fixme("help module loads in admin console", async () => {});
test.fixme("help search returns relevant articles", async () => {});
test.fixme("customer can submit a bug report via concierge chat", async () => {});
