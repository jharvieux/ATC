# dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# next.js build output
.next/
out/

# production build
build/
dist/

# testing
coverage/
playwright-report/
test-results/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# claude code local settings — exclude the local-only contents but keep
# the directory available so .claude/commands/ slash commands (a shared
# workflow artifact per BP31 §32.9) can be tracked.
.claude/settings*.json
.claude/worktrees/
.claude/projects/

# operator's local spec-authoring workspace mirror — not tracked here
/Review/

# typescript
*.tsbuildinfo
next-env.d.ts

# tesseract.js language data — downloaded at first OCR call, not source
*.traineddata
