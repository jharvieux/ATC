# dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# next.js build output
.next/
out/

# production build
build/
dist/

# testing
coverage/
playwright-report/
test-results/
tests/e2e/.auth/

# quality scanners
.lighthouseci/
.stryker-tmp/
reports/mutation/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
*-price-ids.env

# vercel
.vercel

# claude code: settings.json is the SHARED hook + plugin config
# (committed so every contributor / Claude Code session sees the same
# wiring). Per-user overrides go in settings.local.json (gitignored)
# — Claude Code merges the two with local taking precedence.
.claude/settings.local.json
.claude/worktrees/
.claude/projects/

# operator's local spec-authoring workspace mirror — not tracked here
/Review/

# typescript
*.tsbuildinfo
next-env.d.ts

# tesseract.js language data — downloaded at first OCR call, not source
*.traineddata

# Local security-scan tool output + scaffolding (2026-06-05 scan). The findings
# are tracked as issues #715–#757; the raw artifacts are kept local, not in git
# (they enumerate unfixed-vuln detail). Re-generate with the scan tool, not VCS.
/.agents/
/.triage-state/
/.claude/skills/
/skills-lock.json
/apps/main/src/THREAT_MODEL.md
/apps/main/src/VULN-FINDINGS.json
/apps/main/src/VULN-FINDINGS.md

# Vitals provenance data
.vitals/
headroom_memory.db

# Harness session-lock artifact (per-session PID/timestamp) — must not be committed.
/.claude/scheduled_tasks.lock
