# mocked-tenant-tests-baseline.txt — FREEZE-EXISTING baseline for scripts/check-mocked-tenant-tests.ts.
# Burn-down: refs #2028 — tenant-isolation/RLS-claiming tests that mock the Supabase client
# and therefore cannot observe an RLS/grant regression (Harvey M8 rls-mocked-db class; Harvey's
# engagement run counted 53 sites with a claim regex this port deliberately narrows to
# isolation-shaped phrases — see the script header). Line format: <count> <test file>.
# Burn a line down by migrating the tests to the real seeded-DB harness or annotating them
# as unit-scope app-layer-filter tests per #2028's acceptance criteria.
# Baseline burned down to zero by #2028. Keep this empty file so any future
# unannotated claimed-but-mocked test is measured against an explicit zero.
