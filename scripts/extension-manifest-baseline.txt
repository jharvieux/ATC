# extension-manifest-baseline.txt — FREEZE-EXISTING baseline for scripts/check-extension-manifest.ts.
# Burn-down: refs #2000 — PR #2015 (merged) narrowed apps/extension host permissions to the
# connected tenant origin; the broad-host + sensitive-capability combos this guard tracked are
# gone, so the baseline is empty and the guard is zero-tolerance for the extension going forward.
# Line format: <count> <manifest>::<broad-host-pattern>::<capability>.
