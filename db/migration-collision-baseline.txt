# Migration-collision baseline — pre-existing debt accepted as tracked, not
# re-flagged by scripts/check-migration-collision.ts.
#
# Format: <app>:<version>:<sorted,comma,joined,filenames> # reason (issue link required)
# The filename list is part of the key, so a NEW third file landing at an
# already-baselined version is still flagged — this only suppresses the exact
# pre-existing pair/set.
#
# The gate fails on any NEW same-app duplicate or branch-vs-dev collision.
# Fix the underlying migration instead of growing this file.
