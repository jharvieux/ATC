# RLS coverage exceptions
#
# Format: one entry per line as `table_name # reason`
#
# Tables listed here are exempt from the migration lint gate's requirement
# that every tenant_id-bearing table have RLS enabled plus a policy for
# each of SELECT, INSERT, UPDATE, DELETE (per spec §5.1.2 and §30.8).
#
# Only add a table here when the deviation is intentional and reviewed.
# Each entry MUST include a reason.
#
# Examples (illustrative, not active):
#   host_adapters # platform-admin table, not tenant-scoped despite carrying tenant_id
#   audit_log_partitions # append-only, INSERT-only by service role

stripe_webhook_events # tenant_id is nullable (NULL for platform-level events, non-null for Connect tenant events). INSERT/UPDATE/DELETE are exclusively via service_role webhook handlers, which bypass RLS by design per §5.4.1. User-facing SELECT policy restricts visibility to tenant_id IS NOT NULL rows only.
pending_rag_sync # DROPPED in 20260722000019 (contract migration, #1825) — entry retained because migration-lint scans the append-only history where the original CREATE TABLE (RLS enabled, no policies) still lives. Was: platform-internal retry queue, all reads/writes via service_role, no authenticated-user paths per §8.7a.
supervisor_review_queue # platform-internal review queue; SELECT/UPDATE for platform admins only via withPlatformAdminAudit (service_role), INSERT by supervisor sampling path (service_role), DELETE by retention purge (service_role). No authenticated-user policies per §10.5a design.
host_booking_fee_configs # platform-scoped (no tenant_id column). SELECT to authenticated (tenants can read fee catalog). INSERT/UPDATE/DELETE service_role only (platform admin). Per §12.6 design.
host_adapters # platform-scoped adapter registry (no tenant_id column). SELECT to authenticated so tenants can browse adapters. INSERT/UPDATE/DELETE service_role only (platform admin). Per §13.3.
reconciliation_review_queue # platform-admin reconciliation queue. No authenticated-user access paths. All reads and writes via service_role through withPlatformAdminAudit. Per §14.8.
apify_spend_ledger # BP34 §33.9.3 — platform-scoped Apify spend ledger; no tenant_id; service-role-only writes by the ApifyPricingAdapter
pricing_cache # BP33 §33.3 — platform-scoped pricing reference; no tenant_id; service-role-only reads/writes by the adapter
cruisemapper_url_inventory # BP36 §33.5 — platform-scoped URL inventory for the DIY CruiseMapper scraper; no tenant_id; service-role-only writes by the Inngest scraper job
gmail_oauth_tokens # BP34 §34.2 — OAuth tokens written exclusively by the callback route + watch renewal cron via service_role; SELECT to authenticated is enough for the §34.2.4 health endpoint. INSERT/UPDATE/DELETE service_role only.
gmail_inbound_messages # BP34 §34.2 — Pub/Sub webhook persists inbound Gmail messages (no user session); SELECT to authenticated for the §34 review queue context. INSERT/UPDATE/DELETE service_role only.
platform_admins # §26 platform admin roster — no tenant_id (platform-scoped). All authenticated access policies deny; reads/writes are service_role only via assertPlatformAdmin helper. Manual seeding via SQL post-deploy.
tier_definitions # #548 — global reference catalog, no tenant_id (platform-scoped, PLATFORM_READABLE). RLS enabled with zero policies for default-deny over the Data API. All reads are via service-role-backed clients (tenantClient PLATFORM_READABLE passthrough or createServiceRoleClient), which bypass RLS. No authenticated-user read path; USING(true) SELECT policy barred by the lint gate.
pricing_seat_ladder # EPIC #1336 Phase 1 — global agency seat ladder, no tenant_id (platform-scoped, PLATFORM_READABLE). RLS enabled with zero policies = default-deny over the Data API. Service-role-only reads (pricing loader) + writes (Phase 3 editor). No authenticated-user read path; USING(true) barred by the lint gate.
stripe_price_map # EPIC #1336 Phase 2 — Stripe Price-ID map, no tenant_id (platform-scoped). RLS enabled with zero policies = default-deny over the Data API. Service-role-only reads (priceIdFor/loadPriceMap) + writes (backfill + Phase 3 editor). No authenticated-user read path; USING(true) barred by the lint gate.
vendor_health # #786 — platform-scoped vendor health state; no tenant_id. RLS enabled with zero policies = default-deny over the Data API. All reads/writes via service_role (probe upsert + admin page read); no authenticated/SSR read path by design.
personal_access_tokens # #712 — tenant-scoped PAT table; RLS enabled with zero policies = default-deny over the Data API. All reads/writes via service_role only (assertPermission PAT path + token CRUD routes). No authenticated-user PostgREST path; tenant_id isolation enforced in app layer.
destination_images # #1052 — platform-scoped destination image catalog; no tenant_id. RLS enabled with zero policies = default-deny over the Data API. Service-role-only reads/writes by the groups hero-image helper (lib/groups/hero-image.ts).
destination_images_cache # #1052 — platform-scoped per-query image cache; no tenant_id. RLS enabled with zero policies = default-deny over the Data API. Service-role-only reads/writes/upserts by the groups hero-image helper (lib/groups/hero-image.ts).
cruise_lines # platform reference table; no tenant_id column. Shared cruise line catalog across all tenants. Service-role reads/writes by canonical resolver and CruiseMapper scraper.
cruise_ships # platform reference table; no tenant_id column. Shared cruise ship catalog across all tenants. Service-role reads/writes by canonical resolver.
cruise_line_aliases # platform reference table; no tenant_id column. Alias mappings for cruise_lines. Service-role reads by canonical resolver.
cruise_ship_aliases # platform reference table; no tenant_id column. Alias mappings for cruise_ships. Service-role reads by canonical resolver.
personas # platform-level persona definitions; no tenant_id column. Personas are platform-wide, not tenant-scoped. Service-role reads/writes by persona admin and repo.
persona_safety_config # platform-level persona safety configuration; no tenant_id column. Single shared config row keyed by slug. Service-role reads/writes by persona admin.
ai_kill_switch_state # global kill-switch control; no tenant_id column. Singleton row (id=1) toggled by platform admins. Service-role reads by supervisor pipeline.
ai_batch_jobs # per-Anthropic-batch job ledger; no tenant_id column. Each job spans all tenants' requests in that batch by design. Service-role reads/writes by flush and reconcile crons.
