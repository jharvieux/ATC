--- a/apps/rag/src/lib/rate-limit/feedback-limit.ts
+++ b/apps/rag/src/lib/rate-limit/feedback-limit.ts
@@ -1,14 +1,15 @@
 // §6.10 / D-087 — Rate limit for the /api/feedback inbound endpoint.
+// Called ONLY after the HMAC signature has verified (see feedback/route.ts),
+// so the request is already authenticated. The bucket is keyed on a
+// caller-supplied identifier derived from the *verified* body — never on
+// x-forwarded-for, which is attacker-controlled and would let an
+// unauthenticated spoofer share + exhaust the legitimate caller's bucket
+// (and force a pre-auth Redis write) before its signature was ever checked.

 import { getRedis } from "@/lib/redis/client";
@@ export async function checkFeedbackRateLimit(
-  req: Request,
-  secretHint: string,
+  bucketId: string,
 ): Promise<RateLimitResult> {
-  const fwd = req.headers.get("x-forwarded-for") ?? req.headers.get("x-real-ip");
-  const remote = (fwd ?? "unknown").split(",")[0]!.trim();
-  const key = `rl:feedback:${remote}:${secretHint}`;
+  const key = `rl:feedback:${bucketId}`;
   const limit = limitPerMinute();

--- a/apps/rag/src/app/api/feedback/route.ts
+++ b/apps/rag/src/app/api/feedback/route.ts
@@ export async function POST(req: Request): Promise<Response> {
-  // rate limit ran BEFORE the signature check (bug)
-  const secretHint = secret.slice(0, 8);
-  const rl = await checkFeedbackRateLimit(req, secretHint);
-  if (!rl.allowed) {
-    return Response.json({ error: "rate_limited", reset_seconds: rl.reset_seconds },
-      { status: 429, headers: { "Retry-After": String(rl.reset_seconds) } });
-  }
-
+  // Verify the HMAC signature BEFORE anything attacker-influenced (no
+  // rate-limit bucket touch, no Redis write) so an unauthenticated caller
+  // can neither share/exhaust the legitimate bucket nor force a pre-auth incr.
   const rawBody = await req.text();
   const provided = req.headers.get("x-webhook-signature") ?? "";
   const expected = await hmacHex(secret, rawBody);
   if (!timingSafeEqual(provided, expected)) {
     return Response.json({ error: "invalid_signature" }, { status: 401 });
   }
   let parsed;
   try { parsed = bodySchema.parse(JSON.parse(rawBody)); }
   catch { return Response.json({ error: "invalid_body" }, { status: 400 }); }
+
+  // §6.10 rate limit, applied only to authenticated requests. Bucket on the
+  // verified message_id (a value from the signed body) — not spoofable.
+  const rl = await checkFeedbackRateLimit(`msg:${parsed.message_id ?? "global"}`);
+  if (!rl.allowed) {
+    return Response.json({ error: "rate_limited", reset_seconds: rl.reset_seconds },
+      { status: 429, headers: { "Retry-After": String(rl.reset_seconds) } });
+  }

--- a/apps/rag/test/unit/feedback-limit.test.ts
+++ b/apps/rag/test/unit/feedback-limit.test.ts
@@ (update existing #393 fail-closed test to the new single-arg signature)
-    await expect(checkFeedbackRateLimit(req(), "hint")).rejects.toThrow(/REDIS_URL is not set/);
+    await expect(checkFeedbackRateLimit("msg:global")).rejects.toThrow(/REDIS_URL is not set/);
-    const res = await checkFeedbackRateLimit(req(), "hint");
+    const res = await checkFeedbackRateLimit("msg:global");

# Note: error-injection/feedback-webhook.error.test.ts may also call the old
# 2-arg signature — update it too. Recommended new route test: invalid signature
# → 401 AND redis incr never called (proves no pre-auth write).
